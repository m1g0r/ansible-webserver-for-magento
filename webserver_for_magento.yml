# Run with: ansible-playbook -i hosts magento.yml
---
- name: Install WebServer for Magento 
  hosts: all
  sudo: True

- name: Install nginx, PerconaDB PHP-FPM, Varnish, memcached
  hosts: web

  roles:
    - percona
    - nginx
    - php-fpm
    - varnish
    - memcache
